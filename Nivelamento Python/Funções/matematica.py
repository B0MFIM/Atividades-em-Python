# Iniciando nivelamento em python - Antero

# Definição de Somar: recebe x e y, retorna x + y

def somar(x = 0, y = 0):
    return float(x) + float(y)

#OBS: para tornar um diretório inicializável, crie um arquivo em branco chamado __init__.py dentro da pasta que contém sua biblioteca de funções